SlimKing France static site

Fixes in this version:
- Internal links are relative, so pages work both on a custom domain root and on GitHub Pages project paths like /testking/.
- CTA links resolve to the local /play-slimking/ page without hard-coded root paths.
- Existing images were optimized/resized in place; no extra image variants were created.
- Below-fold images use lazy loading and explicit width/height attributes.
- Main hero image is preloaded for faster LCP.

Pages:
- /betting/
- /registration/
- /login/
- /bonus-promo/
- /withdrawal-payouts/
- /free-spins/
- /slots/
- /privacy-policy/
- /cookie/
- /betting-rules/
- /responsible-gaming/
- /aboutus/
- /terms/
- /play-slimking/ redirects after 3 seconds to the affiliate URL

Domain configured in canonical, Open Graph, sitemap and structured data: slimking-france.net.
